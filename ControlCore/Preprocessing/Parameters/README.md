# Preprocessing Parameters

This folder stores parameter files (e.g., MATLAB `.m` scripts or `.mat` files) used to configure the preprocessing Simulink model. These parameters define filter coefficients, signal scaling factors, and other preprocessing settings.
