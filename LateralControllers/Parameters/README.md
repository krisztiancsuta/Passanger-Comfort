# Lateral Controller Parameters

This folder stores parameter and configuration files for the lateral controllers, such as controller gains, weight matrices, constraint definitions, and vehicle model parameters.
